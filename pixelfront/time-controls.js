import{Engine}from"./engine.js";
export function installTimeControls(engine){
  if(!Engine.prototype.timeControlInstalled){Engine.prototype.timeControlInstalled=true;const oldStep=Engine.prototype.step;Engine.prototype.step=function(){this.timeScale??=1;this.timeControlCalls=(this.timeControlCalls||0)+1;if(this.timeScale===0)return;if(this.timeScale===.5&&this.timeControlCalls%2)return;oldStep.call(this);if(this.timeScale===2&&this.running)oldStep.call(this)}}
  engine.timeScale=1;if(typeof document==="undefined")return{show(){},choose(value){engine.timeScale=value}};
  const panel=document.createElement("nav");panel.className="time-controls hidden";panel.setAttribute("aria-label","게임 속도");panel.innerHTML='<button data-speed="0">Ⅱ</button><button data-speed="0.5">0.5×</button><button data-speed="1" class="active">1×</button><button data-speed="2">2×</button>';document.querySelector(".game-shell").append(panel);
  const choose=value=>{engine.timeScale=value;panel.querySelectorAll("button").forEach(button=>button.classList.toggle("active",+button.dataset.speed===value));panel.querySelector('[data-speed="0"]').textContent=value===0?"▶":"Ⅱ"};panel.querySelectorAll("button").forEach(button=>button.onclick=()=>choose(+button.dataset.speed));addEventListener("keydown",event=>{if(event.code==="Space"&&!event.repeat&&event.target===document.body){event.preventDefault();choose(engine.timeScale===0?1:0)}else if(["Digit1","Digit2","Digit3"].includes(event.code))choose(event.code==="Digit1"?.5:event.code==="Digit2"?1:2)});return{show(){panel.classList.remove("hidden")},choose};
}
