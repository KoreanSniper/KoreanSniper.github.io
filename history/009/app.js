const stationList=Object.keys(stations).map(name=>({id:name,name,x:stations[name][0],y:stations[name][1]}));const stationMap=Object.fromEntries(stationList.map(station=>[station.id,station]));const categoryLabels={high_speed:"고속열차",general_train:"일반열차",subway:"지하철",light_rail:"경전철",planned:"개통예정"};function getLineCategory(lineName,line){if(line.planned)return"planned";if(["JDX","WSX","YPX","DDS","DPX","BSS","OBR"].includes(lineName))return"high_speed";if(["NSL","NSL지선"].includes(lineName))return"general_train";if(["신도시경전철","트램","트램지선","국가산업선"].includes(lineName))return"light_rail";return"subway"}const lineCatalog=Object.entries(station_data).map(([lineName,line])=>{const[r,g,b,a]=line.color;return{lineName,...line,category:getLineCategory(lineName,line),colorCss:`rgba(${r*255},${g*255},${b*255},${a})`}});const availableCategoryEntries=Object.entries(categoryLabels).filter(([key])=>lineCatalog.some(line=>line.category===key));const lines=[];for(const line of lineCatalog){for(let i=0;i<line.stations.length-1;i++){lines.push({from:line.stations[i],to:line.stations[i+1],color:line.colorCss,lineName:line.lineName,planned:Boolean(line.planned),category:line.category})}}const OVERLAP_TOLERANCE=.01;const activeCategories=new Set(Object.keys(categoryLabels));const lineElements=[];const stationElements=[];const labelElements=[];const badgeElements=[];let scale=1,ox=0,oy=0,drag=false,sx=0,sy=0,highlightedStation=null,focusedLineName=null;const DENSE_CORE_BOUNDS={minX:180,maxX:860,minY:-80,maxY:430};const viewport=document.getElementById("viewport"),svg=document.getElementById("map"),stationSearchInput=document.getElementById("stationSearch"),lineSearchInput=document.getElementById("lineSearch"),startInput=document.getElementById("start"),viaInput=document.getElementById("via"),endInput=document.getElementById("end"),stationOptions=document.getElementById("stationOptions"),lineOptions=document.getElementById("lineOptions"),categoryFilters=document.getElementById("categoryFilters"),lineGroups=document.getElementById("lineGroups"),resultBox=document.getElementById("resultBox"),findRouteBtn=document.getElementById("findRouteBtn"),resetViewBtn=document.getElementById("resetViewBtn");function createLineGeometry(line){const fromStation=stationMap[line.from],toStation=stationMap[line.to],dx=toStation.x-fromStation.x,dy=toStation.y-fromStation.y,length=Math.hypot(dx,dy)||1;let unitX=dx/length,unitY=dy/length;if(unitX<0||Math.abs(unitX)<=OVERLAP_TOLERANCE&&unitY<0){unitX*=-1;unitY*=-1}return{...line,x1:fromStation.x,y1:fromStation.y,x2:toStation.x,y2:toStation.y,length,travelUnitX:dx/length,travelUnitY:dy/length,unitX,unitY,normalX:-unitY,normalY:unitX}}function getLineProjectionRange(line){const start=line.x1*line.unitX+line.y1*line.unitY,end=line.x2*line.unitX+line.y2*line.unitY;return[Math.min(start,end),Math.max(start,end)]}function isSameDirection(a,b){return Math.abs(a.unitX-b.unitX)<=OVERLAP_TOLERANCE&&Math.abs(a.unitY-b.unitY)<=OVERLAP_TOLERANCE}function isCollinear(a,b){const cross=(b.x1-a.x1)*a.unitY-(b.y1-a.y1)*a.unitX;return Math.abs(cross)<=OVERLAP_TOLERANCE}function hasOverlappingRange(a,b){const[aStart,aEnd]=getLineProjectionRange(a),[bStart,bEnd]=getLineProjectionRange(b);return Math.min(aEnd,bEnd)-Math.max(aStart,bStart)>OVERLAP_TOLERANCE}function isOverlappingSegment(a,b){return isSameDirection(a,b)&&isCollinear(a,b)&&hasOverlappingRange(a,b)}function getParallelOffset(index,total,gap=10){return(index-(total-1)/2)*gap}function getRenderedSegmentPoints(line,overlapIndex,overlapCount){const offset=getParallelOffset(overlapIndex,overlapCount),taper=Math.min(line.length*.3,24);if(Math.abs(offset)<=OVERLAP_TOLERANCE||line.length<=taper*2)return[[line.x1,line.y1],[line.x2,line.y2]];const innerStartX=line.x1+line.travelUnitX*taper,innerStartY=line.y1+line.travelUnitY*taper,innerEndX=line.x2-line.travelUnitX*taper,innerEndY=line.y2-line.travelUnitY*taper;return[[line.x1,line.y1],[innerStartX+line.normalX*offset,innerStartY+line.normalY*offset],[innerEndX+line.normalX*offset,innerEndY+line.normalY*offset],[line.x2,line.y2]]}function getUnitVector(fromStationId,toStationId){const fromStation=stationMap[fromStationId],toStation=stationMap[toStationId],dx=toStation.x-fromStation.x,dy=toStation.y-fromStation.y,length=Math.hypot(dx,dy)||1;return{x:dx/length,y:dy/length}}function getBadgeLabel(lineName){const compactMap={"중앙권순환철도":"중앙순환","신도시경전철":"신도시","트램지선":"트램지","국가산업선":"산업","9호선":"9","9호선지선":"9지","9호선지선 예정구간":"9지예정","동영2호선지선":"동영2지","유신선":"유신선"};if(compactMap[lineName])return compactMap[lineName];const trimmed=lineName.replace(/호선/g,"").replace(/계획선/g,"계획").replace(/연장계획/g,"연장").trim();if(trimmed.length<=5)return trimmed;return trimmed.slice(0,5)}function getBadgeFontSize(label){if(label.length<=2)return 14;if(label.length===3)return 11;if(label.length===4)return 9;return 8}function isDenseCoreStation(name){const station=stationMap[name];return station&&station.x>=DENSE_CORE_BOUNDS.minX&&station.x<=DENSE_CORE_BOUNDS.maxX&&station.y>=DENSE_CORE_BOUNDS.minY&&station.y<=DENSE_CORE_BOUNDS.maxY}function createLineBadge(lineName,stationName,direction,color,planned,category){const station=stationMap[stationName],label=getBadgeLabel(lineName),fontSize=getBadgeFontSize(label),badgeOffset=34,badgeRadius=18,cx=station.x+direction.x*badgeOffset,cy=station.y+direction.y*badgeOffset,group=document.createElementNS("http://www.w3.org/2000/svg","g"),circle=document.createElementNS("http://www.w3.org/2000/svg","circle"),text=document.createElementNS("http://www.w3.org/2000/svg","text");circle.setAttribute("cx",cx);circle.setAttribute("cy",cy);circle.setAttribute("r",badgeRadius);circle.setAttribute("fill",color);circle.setAttribute("stroke","#e2e8f0");circle.setAttribute("stroke-width","3");if(planned)circle.setAttribute("stroke-dasharray","6 4");text.setAttribute("x",cx);text.setAttribute("y",cy+2);text.setAttribute("text-anchor","middle");text.setAttribute("dominant-baseline","middle");text.setAttribute("class","line-badge-text");text.setAttribute("font-size",String(fontSize));text.textContent=label;group.appendChild(circle);group.appendChild(text);viewport.appendChild(group);group.style.cursor="pointer";group.addEventListener("click",()=>focusOnLine(lineName));badgeElements.push({element:group,category,stationName,lineName})}function buildGraph(){const graph={};stationList.forEach(station=>{graph[station.id]=[]});lines.forEach(line=>{if(line.planned||!activeCategories.has(line.category))return;graph[line.from].push({to:line.to,cost:1});graph[line.to].push({to:line.from,cost:1})});return graph}function dijkstra(start,end){const graph=buildGraph();if(!graph[start]||!graph[end])return null;const dist={},prev={},queue=[];Object.keys(graph).forEach(node=>{dist[node]=Infinity;prev[node]=null});dist[start]=0;queue.push({node:start,cost:0});while(queue.length){queue.sort((a,b)=>a.cost-b.cost);const{node}=queue.shift();graph[node].forEach(next=>{const newCost=dist[node]+next.cost;if(newCost<dist[next.to]){dist[next.to]=newCost;prev[next.to]=node;queue.push({node:next.to,cost:newCost})}})}if(dist[end]===Infinity)return null;const path=[];let current=end;while(current){path.unshift(current);current=prev[current]}return path}function parseViaStations(value){return value.split(",").map(name=>name.trim()).filter(Boolean)}function buildRoutePath(stationNames){const fullPath=[];for(let i=0;i<stationNames.length-1;i++){const segment=dijkstra(stationNames[i],stationNames[i+1]);if(!segment)return null;if(i===0)fullPath.push(...segment);else fullPath.push(...segment.slice(1))}return fullPath}function setResult(message,emphasize=false){resultBox.innerHTML=emphasize?`<strong>${message}</strong>`:message}function syncCategoryButtons(){categoryFilters.querySelectorAll("[data-category]").forEach(button=>{button.classList.toggle("active",activeCategories.has(button.dataset.category))})}function applyLineFocus(){if(!focusedLineName)return;lineElements.forEach(({element,line})=>{if(!activeCategories.has(line.category))return;if(line.lineName===focusedLineName){element.setAttribute("stroke-opacity","1");element.setAttribute("stroke-width","10")}else{element.setAttribute("stroke-opacity",line.planned?"0.22":"0.15");element.setAttribute("stroke-width","6")}})}function clearHighlights(){lineElements.forEach(({element,line})=>{element.setAttribute("stroke",line.color);element.setAttribute("stroke-opacity",line.planned?"0.9":"1");element.setAttribute("stroke-width","8")});applyLineFocus()}function highlight(path){clearHighlights();for(let i=0;i<path.length-1;i++){const a=path[i],b=path[i+1];lineElements.forEach(({element,line})=>{if(line.from===a&&line.to===b||line.from===b&&line.to===a){element.setAttribute("stroke","#facc15");element.setAttribute("stroke-opacity","1")}})}}function getVisibleStationSet(){const visibleStations=new Set;lines.forEach(line=>{if(!activeCategories.has(line.category))return;visibleStations.add(line.from);visibleStations.add(line.to)});return visibleStations}function updateVisibility(){const visibleStations=getVisibleStationSet();lineElements.forEach(({element,line})=>{element.style.display=activeCategories.has(line.category)?"":"none"});badgeElements.forEach(({element,category,stationName,lineName})=>{const visible=activeCategories.has(category)&&(!isDenseCoreStation(stationName)||scale>=1.45||stationName===highlightedStation||focusedLineName===lineName);element.style.display=visible?"":"none";element.style.opacity=focusedLineName&&lineName!==focusedLineName?"0.28":"1"});stationElements.forEach(({name,element})=>{const visible=visibleStations.has(name);element.style.display=visible?"":"none";element.classList.toggle("dimmed",!visible);element.classList.toggle("highlighted",name===highlightedStation)});labelElements.forEach(({name,element})=>{const visible=visibleStations.has(name);const showLabel=!isDenseCoreStation(name)||scale>=1.2||name===highlightedStation;element.style.display=visible?"":"none";element.classList.toggle("dimmed",!visible);element.classList.toggle("compact",visible&&!showLabel)});updateLineGroupState()}function focusOnStations(path){const points=path.map(name=>stationMap[name]).filter(Boolean);if(!points.length)return;const minX=Math.min(...points.map(point=>point.x)),maxX=Math.max(...points.map(point=>point.x)),minY=Math.min(...points.map(point=>point.y)),maxY=Math.max(...points.map(point=>point.y)),width=Math.max(maxX-minX,120),height=Math.max(maxY-minY,120),availableWidth=svg.clientWidth-140,availableHeight=svg.clientHeight-140;scale=Math.max(.45,Math.min(2.4,Math.min(availableWidth/width,availableHeight/height)));ox=svg.clientWidth/2-(minX+maxX)/2*scale;oy=svg.clientHeight/2-(minY+maxY)/2*scale;update()}function focusOnStation(name){const station=stationMap[name];if(!station)return;focusedLineName=null;lineSearchInput.value="";highlightedStation=name;ox=svg.clientWidth/2-station.x*scale;oy=svg.clientHeight/2-station.y*scale;updateVisibility();clearHighlights();updateLineGroupState();update()}function focusOnLine(name){const line=lineCatalog.find(item=>item.lineName===name);if(!line){setResult("검색한 노선을 찾지 못했습니다.",true);return}focusedLineName=name;highlightedStation=null;activeCategories.add(line.category);syncCategoryButtons();updateVisibility();clearHighlights();focusOnStations(line.stations);setResult(`<strong>${line.lineName}</strong><br>${categoryLabels[line.category]} / ${line.stations.length}개 역<br>${line.stations.join(" → ")}`);updateLineGroupState()}function findRoute(){const startName=startInput.value.trim(),endName=endInput.value.trim(),viaNames=parseViaStations(viaInput.value),routeNames=[startName,...viaNames,endName];if(!startName||!endName){setResult("출발역과 도착역을 모두 입력해 주세요.",true);return}const invalidStation=routeNames.find(name=>!stationMap[name]);if(invalidStation){setResult(`입력한 역을 찾을 수 없습니다: ${invalidStation}`,true);return}focusedLineName=null;lineSearchInput.value="";const path=buildRoutePath(routeNames);if(!path){setResult("현재 선택된 분류 안에서 연결 경로를 찾지 못했습니다. 필터를 넓히거나 역 이름을 다시 확인해 주세요.",true);clearHighlights();return}highlightedStation=null;highlight(path);focusOnStations(path);const viaText=viaNames.length?` / 경유 ${viaNames.join(" → ")}`:"";setResult(`최단 경로 ${path.length-1}구간<br>${startName}${viaText} → ${endName}<br>${path.join(" → ")}`);updateVisibility()}function resetViewState(){scale=1;ox=0;oy=0;highlightedStation=null;focusedLineName=null;clearHighlights();updateVisibility();updateLineGroupState();update();setResult("출발역과 도착역을 입력하면 최단 경로가 여기에 표시됩니다.")}function renderStationOptions(){stationOptions.innerHTML=stationList.slice().sort((a,b)=>a.name.localeCompare(b.name,"ko")).map(station=>`<option value="${station.name}"></option>`).join("")}function renderLineOptions(){lineOptions.innerHTML=lineCatalog.slice().sort((a,b)=>a.lineName.localeCompare(b.lineName,"ko")).map(line=>`<option value="${line.lineName}"></option>`).join("")}function renderCategoryFilters(){categoryFilters.innerHTML=availableCategoryEntries.map(([key,label])=>`<button class="chip active" type="button" data-category="${key}">${label}</button>`).join("");categoryFilters.querySelectorAll("[data-category]").forEach(button=>{button.addEventListener("click",()=>{const category=button.dataset.category;if(activeCategories.has(category))activeCategories.delete(category);else activeCategories.add(category);button.classList.toggle("active",activeCategories.has(category));clearHighlights();updateVisibility();updateLineGroupState()})})}function updateLineGroupState(){lineGroups.querySelectorAll(".line-pill[data-line]").forEach(pill=>{const category=pill.dataset.category;const lineName=pill.dataset.line;const inactive=!activeCategories.has(category);const mismatched=focusedLineName&&lineName!==focusedLineName;pill.classList.toggle("dimmed",inactive||mismatched);pill.classList.toggle("active",focusedLineName===lineName)})}function renderLineGroups(){lineGroups.innerHTML=availableCategoryEntries.map(([key,label])=>{const groupLines=lineCatalog.filter(line=>line.category===key),pills=groupLines.map(line=>`<div class="line-pill" data-line="${line.lineName}" data-category="${line.category}"><span class="swatch" style="background:${line.colorCss}"></span><span>${line.lineName}</span></div>`).join("");return`<section class="line-group"><h3>${label}</h3><div class="line-list">${pills||'<div class="line-pill">없음</div>'}</div></section>`}).join("");lineGroups.querySelectorAll(".line-pill[data-line]").forEach(pill=>{pill.addEventListener("click",()=>focusOnLine(pill.dataset.line))});updateLineGroupState()}function update(){viewport.setAttribute("transform",`translate(${ox},${oy}) scale(${scale})`)}const renderedLines=lines.map(createLineGeometry),overlapGroups=renderedLines.map(()=>[]);for(let i=0;i<renderedLines.length;i++){overlapGroups[i].push(i);for(let j=0;j<renderedLines.length;j++){if(i!==j&&isOverlappingSegment(renderedLines[i],renderedLines[j]))overlapGroups[i].push(j)}overlapGroups[i].sort((a,b)=>renderedLines[a].lineName!==renderedLines[b].lineName?renderedLines[a].lineName.localeCompare(renderedLines[b].lineName,"ko"):a-b)}renderedLines.forEach((line,index)=>{const overlapGroup=overlapGroups[index],overlapIndex=overlapGroup.indexOf(index),points=getRenderedSegmentPoints(line,overlapIndex,overlapGroup.length),el=document.createElementNS("http://www.w3.org/2000/svg","polyline");el.setAttribute("points",points.map(([x,y])=>`${x},${y}`).join(" "));el.setAttribute("stroke",line.color);el.setAttribute("stroke-width",8);el.setAttribute("stroke-linecap","round");el.setAttribute("stroke-linejoin","round");el.setAttribute("fill","none");if(line.planned){el.setAttribute("stroke-dasharray","16 12");el.setAttribute("stroke-opacity","0.9")}viewport.appendChild(el);lineElements.push({element:el,line})});lineCatalog.forEach(line=>{if(!line.stations||line.stations.length<2)return;const startStation=line.stations[0],startNeighbor=line.stations[1],endStation=line.stations[line.stations.length-1],endNeighbor=line.stations[line.stations.length-2],startDirection=getUnitVector(startNeighbor,startStation),endDirection=getUnitVector(endNeighbor,endStation);createLineBadge(line.lineName,startStation,startDirection,line.colorCss,Boolean(line.planned),line.category);if(!(line.is_loop&&startStation===endStation))createLineBadge(line.lineName,endStation,endDirection,line.colorCss,Boolean(line.planned),line.category)});stationList.forEach(station=>{const circle=document.createElementNS("http://www.w3.org/2000/svg","circle");circle.setAttribute("cx",station.x);circle.setAttribute("cy",station.y);circle.setAttribute("r",10);circle.setAttribute("class","station");circle.setAttribute("stroke","#22c55e");circle.onmouseenter=()=>circle.setAttribute("r",14);circle.onmouseleave=()=>circle.setAttribute("r",10);circle.onclick=()=>{if(!startInput.value)startInput.value=station.name;else if(!endInput.value||endInput.value===station.name)endInput.value=station.name;else stationSearchInput.value=station.name;highlightedStation=station.name;focusOnStation(station.name);setResult(`<strong>${station.name}</strong><br>선택한 역으로 지도를 이동했습니다. 출발역과 도착역 입력에도 바로 사용할 수 있습니다.`)};viewport.appendChild(circle);stationElements.push({name:station.name,element:circle});const text=document.createElementNS("http://www.w3.org/2000/svg","text");text.setAttribute("x",station.x);text.setAttribute("y",station.y+20);text.setAttribute("text-anchor","middle");text.setAttribute("class","label");text.textContent=station.name;viewport.appendChild(text);labelElements.push({name:station.name,element:text})});findRouteBtn.addEventListener("click",findRoute);resetViewBtn.addEventListener("click",()=>{startInput.value="";viaInput.value="";endInput.value="";stationSearchInput.value="";resetViewState()});stationSearchInput.addEventListener("change",()=>{const stationName=stationSearchInput.value.trim();if(!stationMap[stationName]){setResult("검색한 역을 찾지 못했습니다.",true);return}focusOnStation(stationName);setResult(`<strong>${stationName}</strong><br>지도 중심을 해당 역으로 이동했습니다.`)});lineSearchInput.addEventListener("change",()=>{const lineName=lineSearchInput.value.trim();if(!lineName)return;focusOnLine(lineName)});[startInput,viaInput,endInput].forEach(input=>{input.addEventListener("keydown",event=>{if(event.key==="Enter"){event.preventDefault();findRoute()}})});svg.onmousedown=event=>{drag=true;sx=event.clientX-ox;sy=event.clientY-oy};svg.onmousemove=event=>{if(!drag)return;ox=event.clientX-sx;oy=event.clientY-sy;update()};svg.onmouseup=()=>{drag=false};svg.onmouseleave=()=>{drag=false};svg.onwheel=event=>{event.preventDefault();scale+=event.deltaY>0?-.1:.1;scale=Math.max(.45,Math.min(3,scale));update()};renderStationOptions();renderLineOptions();renderCategoryFilters();renderLineGroups();updateVisibility();clearHighlights();updateLineGroupState();update();
